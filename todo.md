如果希望实现自动入库，比较安全的方案不是直接把所有候选写进去，而是在日报生成后增加一个受控步骤：
读取“概念候选”附录
→ 与 concepts.json 的 id/name/aliases 去重
→ 过滤营销词、产品名和低置信候选
→ 只追加最小 candidate
→ JSON 校验
→ 不自动生成口播、不自动晋升 ready
这样既能减少人工搬运，又不会让未经裁定的内容污染成品库。



日报候选负责发现新词
    +
term-frequency 负责验证长期价值
    ↓
维护 Agent 决定是否进入 concepts.json





1、手动入库
读取手动入库.md 入完库前面打勾

2、查看各个生命周期的概念
candidate → draft → ready → used/stale
说明每种状态的进入条件和禁止操作。

3、展示待审核的概念

4、生成月报的时候 运行的提取脚本


